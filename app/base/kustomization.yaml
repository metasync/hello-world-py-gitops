apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - httproute.yaml
  - configmap.yaml

labels:
  - pairs:
      app.kubernetes.io/name: hello-world-py
      app.kubernetes.io/part-of: metasync
    includeSelectors: true
    includeTemplates: true
